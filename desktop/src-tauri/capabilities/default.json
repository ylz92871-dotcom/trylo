{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Trylo Desktop main window. Grants the webview access to the custom file system + git commands registered in src-tauri/src/commands/.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
