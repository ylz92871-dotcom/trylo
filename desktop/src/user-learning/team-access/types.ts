// Trylo Team Access — shared types barrel (PR-1).
//
// Later PRs append their types here: TeamSpawnDecision (PR-3),
// PersonSeatOutput (PR-5), TeamClarificationRequest (PR-5),
// TeamTranslation* (PR-4). Contract types live in contract-types.ts so
// no file exceeds the ~400-line readability budget (spec §20.7).
export * from './contract-types';
